# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-finance-container
metadata:
  template: mcp-container-python@1.0.0

infra:
  provider: bicep
  path: infra/container-app

services:
  mcp-server:
    project: ./container-app
    language: python
    host: containerapp
    docker:
      path: ./container-app/Dockerfile
      context: ./container-app
