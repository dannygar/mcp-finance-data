# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mcp-finance-container
metadata:
  template: mcp-container-python@1.0.0

infra:
  provider: bicep
  path: infra/mcp-server

services:
  mcp-server:
    project: mcp-server
    language: python
    host: containerapp
    docker:
      path: Dockerfile
      context: .
